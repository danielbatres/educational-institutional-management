@page "/application/{InstitutionId}/students"
@layout ApplicationLayout

<PageTitle>Aplicaci&oacute;n | Estudiantes</PageTitle>

<Students />

@code {
  [Parameter]
  public string InstitutionId { get; set; }
  [Inject]
  private SideBarContext SideBarContext { get; set; }
  [Inject]
  private ContentContext ContentContext { get; set; }

  protected override void OnInitialized() {
    SideBarContext.SetSelectedOptionMainMenu(1);
    ContentContext.SetSectionContent("Estudiantes", "Ver estudiantes");
  }
}