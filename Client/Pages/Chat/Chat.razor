@page "/application/{InstitutionId}/chat"
@layout ApplicationLayout

<PageTitle>Aplicaci&oacute;n | Comunicaci&oacute;n</PageTitle>

<GeneralChat />

@code {
  [Parameter]
  public string InstitutionId { get; set; }
  [Inject]
  private ContentContext ContentContext { get; set; }
  [Inject]
  private SideBarContext SideBarContext { get; set; }

  protected override void OnInitialized() {
    SideBarContext.SetSelectedOptionMainMenu(6);
    ContentContext.SetSectionContent("Comunicación", "Chat general");
  }
}