@page "/application/{InstitutionId}/activity"
@layout ApplicationLayout

<PageTitle>Aplicaci&oacute;n | Registro de actividad</PageTitle>

<Activity />

@code {
  [Parameter]
  public string InstitutionId { get; set; }
  [Inject]
  private SideBarContext SideBarContext { get; set; }
  [Inject]
  private ContentContext ContentContext { get; set; }

  protected override void OnInitialized() {
    SideBarContext.SetSelectedOptionMainMenu(5);
    ContentContext.SetSectionContent("Actividad", "Registro de actividad");
  }
}